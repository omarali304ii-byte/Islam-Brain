# Next Steps — cielito-egypt

## Immediate (this estate, next session)
1. **Build the hero dashboard** — `Mega Run cielito-egypt wave hero` → scaffolds `build_cielito_data.py` (fail-closed compiler) + React components → deploys `esm-landing /dashboard/cielito-360`. This is the WOM pitch centerpiece; spec is ready in `dashboard/react_dashboard_spec.md`.
2. **Backfill internal era coverage** — re-run the era fan-out (13 ATLAS + 8 GTM + 1 v6 lanes) after credit reset (8:10pm Cairo) or in a fresh session. Resume: `Workflow({scriptPath: ".../cielito-base360-era-fanout-*.js", resumeFromRunId})`. Not a client deliverable, but completes the 360 estate.
3. **Complete `strategy/MARKETING_STRATEGY.md` prose** — strategy.json is the binding artifact (schema-PASS) and the deck/dashboard read it; the long-form prose write-up is the remaining nicety.

## Waves queued (Executive lens order)
`ceo-redesign` (dashboard IA already executive-shelled) → `competitive` (needs P1 data pass) → `hero` (dashboard build) → `benchmark`. GIS wave SKIPPED (no branch network — online D2C).

## Client asks (close the GAPs no scrape can)
- Shopify export + GA4/Insights access → converts every "to be baselined" card into ROI scenarios (G-01, G-03).
- 60-min founder session → fruit-leather status + tagline lock (G-04) + second-domain purpose (G-05).

## Convert to paid engagement
Bridge to `marketing-build-strategy` / `niq-proposal` to turn this 360 + dashboard into WOM's priced proposal for Cielito.

---

## Data-Pass Menu (base 360 close)
Full menu with costed routes and recorded decisions: **`_intel/data_pass_menu_base360.md`**. Estate spend to date **$0.434 / $25**. Top 3 recommended: (1) request client order data — free; (2) `approve P2` follower-quality audit (~$1–3); (3) `approve P1` competitive social pull (~$1.5–2.5).
